# --- PRODUCTION SERVER CONFIGURATION ---
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=thedilution_user
DB_PASSWORD=admin123
DB_NAME=thedilution_db

# The internal port your Node app listens on for the Apache proxy
PORT=8080

# --- THIS IS THE CRITICAL LINE ---
# The API must call the robot's public domain name.
ROBOT_API_URL=http://localhost:8082/api/

JWT_SECRET=your_super_secret_key