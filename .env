# --- The One Standard Configuration ---

# The HOST - Use 'db' for Docker, 'localhost' for local development
DB_HOST=localhost

# The PORT is the database's internal port.
DB_PORT=3307

DB_USER=thedilution_user
DB_PASSWORD=admin123
DB_NAME=thedilution_db

# This is the port your Node.js app runs on INSIDE the container.
PORT=8080

# --- Security ---
JWT_SECRET=a_very_strong_and_secret_key_that_no_one_can_guess